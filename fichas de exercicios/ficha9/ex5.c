#include <stdio.h>
        
float a,b,c,resultado;

int main()
{
    resultado  =  a +b;
     if (resultado <  c)
     {
        printf(" O resultado da Soma de A + B é menor que C");
        printf("A : %.2f\n", a);


     }

}